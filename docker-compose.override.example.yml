# Optional local overrides. Copy to docker-compose.override.yml
#
# Expose web + API directly (e.g. http://localhost:3000) while nginx still serves https://localhost.
# Set NEXT_PUBLIC_API_URL=http://localhost:8000 in .env for this mode.

# services:
#   web:
#     ports:
#       - "3000:3000"
#   api:
#     ports:
#       - "8000:8000"
